#https://github.com/certbot/certbot/issues/5236
server {
    server_name glc-oem.greatlakescode.us;
    root  /home/ubuntu/public;

    location / {
        try_files $uri @backend;
    }



    location @backend {
        proxy_pass http://127.0.0.1:5006;
    }

}




server {
    underscores_in_headers on;
    server_name russjohnson09-oem.greatlakescode.us;
    root  /home/ubuntu/public;


    location /api/v1/webhook {
        auth_basic          off;
        try_files $uri @backend;
    }

    location / {
        try_files $uri @backend;
    }

    location @backend {
        allow 127.0.0.1;
        allow 0.0.0.0;
        allow 96.85.110.86/32;
        deny all;
        auth_basic           "Administrator’s Area";
        auth_basic_user_file /etc/nginx/.oem-pass;
        proxy_pass http://127.0.0.1:5007;
    }

}